apiVersion: v1
kind: Service
metadata:
  name: blog-frontend
  namespace: default
  labels:
    app: blog-frontend
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: blog-frontend
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
