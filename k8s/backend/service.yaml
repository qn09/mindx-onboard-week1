apiVersion: v1
kind: Service
metadata:
  name: blog-backend
  namespace: default
  labels:
    app: blog-backend
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: blog-backend
  ports:
  - name: http
    port: 3001
    targetPort: 3001
    protocol: TCP
  sessionAffinity: None
